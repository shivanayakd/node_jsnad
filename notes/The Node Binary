node --help    // To see all Node command line flags 
node --v8-options // additional flags for modifying the JavaScript runtime engine: V8
node --check/-c app.js  // parse a JavaScript application without running it in order to just check the syntax.

node -print/-p "1+1"   // evaluates an expression and prints the result
node -eval/-e "1+1"   // evaluates without printing the result of the expression

node --require/-r ./preload.js app.js   // used to preload a CommonJS module before anything else loads.

node --stack-trace-limit=101 app.js   // stack trace limit to a number higher than the amount of call frames in the stack guarantees that the entire stack will be output: